# Public (exposed to client-side bundles — used by Socket.IO client)
PUBLIC_API_URL=https://your-backend.up.railway.app

# Private (server-side only — used by Astro API proxy routes)
API_URL=https://your-backend.up.railway.app

# Railway sets PORT and HOST automatically
# HOST=0.0.0.0
# PORT=4321
