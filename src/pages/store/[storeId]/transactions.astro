---
import AppLayout from '../../../layouts/AppLayout.astro';
import { TransactionManager } from '../../../components/app/TransactionManager';

const { storeId } = Astro.params;
---

<AppLayout title="Transactions" storeId={storeId}>
  <TransactionManager storeId={storeId!} client:load />
</AppLayout>
